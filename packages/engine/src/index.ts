// Nanchang Mahjong game engine — pure TypeScript, no I/O, fully deterministic.
// Phase 5 will implement the full rules: tile set, hand evaluation, scoring (fan),
// call resolution, wildcard (Jing) mechanics, and the move-log emitter for replays.

export const ENGINE_VERSION = '0.0.1';
